enabled: true
backends:
  # No "url" here on purpose: this entry means whatever this build already ships against
  # (kDefaultServerUrl in lib/server_config.dart), so the deployed address is written down once.
  # Add further entries with an explicit "url" to point a debug build somewhere else.
  - name: "Cloud Run (karata0)"
